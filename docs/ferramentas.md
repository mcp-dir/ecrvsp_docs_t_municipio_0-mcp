# Ferramentas

ECRVSP Documentos: Transferência de Município expõe 1 ferramenta (todas somente leitura).

### 1. `ecrvsp_docs_t_municipio_0_consultar`
**Input**: `a3`, `a3_pin`, `login_cpf`, `login_senha`, `chassi`, `file_pdf` (opcional), `categoria`, `tipo_restricao`, `cpf` (opcional), `cnpj` (opcional), `rg` (opcional), `rg_orgao_expedidor` (opcional), `rg_uf` (opcional), `numero_espelho`, `endereco_cep`, `endereco_numero`, `endereco_complemento` (opcional), `nome`, `nome_anterior`, `telefone` (opcional), `tipo_placa_dianteira`, `tipo_placa_traseira`, `tipo_placa_traseira_segunda`

ECRVSP Documentos: Transferência de Município, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar ECRVSP Documentos: Transferência de Município
```
